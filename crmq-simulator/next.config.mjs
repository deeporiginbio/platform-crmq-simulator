/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/platform-crmq-simulator/crmq-simulator',
  images: {
    unoptimized: true,
  },
};
export default nextConfig;
